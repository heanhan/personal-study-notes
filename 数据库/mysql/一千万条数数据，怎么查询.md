### 一千万条数据怎么查询

#### 结论

```
对于一千万条数据，主要关注分页查询过程中的性能
两个方面：

一方面： 针对偏移量大导致的查询速度慢
1、先对查询的字段创建唯一索引
2、根据业务需求，先定位查询的范围（对应逐渐的id范围，比如大于多少、小于多少、in）
3、查询时将第2步的范围作为查询条件。

第二方面：针对数据量大的导致查询速度慢
1、查询是，减少不需要的列，查询效率也可以得到明显的提升
2、一次尽可能按需查询较少的数据条数
3、借助nosql缓存数据来减轻mysql的数据库压力

```

